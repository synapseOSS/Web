# Agent Rules

- Do not create markdown files for summaries or documentation unless explicitly requested
- Do not create SQL files, instead execute SQL directly with Supabase MCP tools
- Keep consistency in overall product theme
